/**
 * Canonical Timeline component (per project plan).
 * Thin alias over EditorTimeline so `components/editor/Timeline.tsx` is the
 * importable name used by pages.
 */
export { EditorTimeline as Timeline } from "./EditorTimeline";
export type { EditorTimelineProps } from "./EditorTimeline";
